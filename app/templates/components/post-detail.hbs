<div class="row">
  <div>{{post.title}}</div>
  <img src={{post.image}} alt={{post.title}}>
  <p>{{post.author}} - {{post.date}}</p>
</div>

<div class="row">
  <p>{{post.content}}</p>
</div>

<div class="row">
  <p>Tags: {{post.category}}</p>
</div>
