{{#if updateCommentForm}}
    <div>
      <form>
        <div class="form-group">
          <label for="name">Name</label>
          {{input value=name id="name" placeholder=comment.name}}
        </div>

        <div class="form-group">
          <label for="email">Email</label>
          {{input value=email id="email" placeholder=comment.email}}
        </div>

        <div class="form-group">
          <label for="text">Text</label>
          {{input value=text id="text" placeholder=comment.text}}
        </div>

        <button class="btn btn-default" {{action 'update' comment}}>Save</button>
      </form>
    </div>
{{else}}
    <button class="btn btn-default" {{action 'updateCommentForm'}}><span class="glyphicon glyphicon-plus"></span></button>
{{/if}}
